# Worker Threads

**How to use:**

```ts
import throng from '@reignmodule/throng';

const runServer = () => { ... }

throng(runServer);
// Server ready on port 3000
// Server ready on port 3000
// Server ready on port 3000
// Server ready on port 3000
```
